# Workspace assets

Arquivos de referência **não servidos pelo Next** (`public/`). Usados em build/runtime server-side.

| Caminho | Consumidor |
|---------|------------|
| `tickets/reference-images/image-ticket.png` | `apps/web/lib/events/event-ticket-pdf.ts` |

Ao adicionar novos assets de referência, documentar aqui o path e o módulo que lê o arquivo.
