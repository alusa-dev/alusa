{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["gru1"],
  "installCommand": "npm ci --workspaces=false --ignore-scripts",
  "buildCommand": "npm run build",
  "framework": "nextjs"
}
